<% for user in @users %>
  <div class="user">
    <p>
      <strong><%=h user.username %></strong>
      <%= link_to "Add Match", matches_path(:match_id => user), :method => :post %>
      <div class="clear"></div>
    </p>
  </div>
<% end %>
